This directory is for LSL scripts, sounds and textures which are shared among multiple objects.
Assets that are only used by a single type of object should saved in the mod/<modname>/objects/<objectcode>/assets directory.
Assets that are shared, but only by object using the same mod/ should saved in the mod/<modname>/assets directory.
